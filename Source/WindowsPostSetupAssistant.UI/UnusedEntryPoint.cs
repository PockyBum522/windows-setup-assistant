namespace WindowsPostSetupAssistant.UI;

// ReSharper disable once ClassNeverInstantiated.Global because this is just to keep the compiler happy
public class UnusedEntryPoint
{
    public static void Main()
    {
        // This is just to keep the compiler happy
        
        // We're basically using this as a library
        
        // But for WPF stuff to work it has to be set as WinExe in the project type
    }
}